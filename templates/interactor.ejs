//import { useEffect } from 'react';
import Presenter from './<%=name%>Presenter';
//import { useAppSelector, useAppDispatch, myReducerName } from '../../controllers';
//import { TMProps<%=(name.charAt(0).toUpperCase() + name.slice(1))%> } from './<%=name%>Type';
//import {} from '../../types';

const Interactor = () => {
  //const dispatch = useAppDispatch();

  //useEffect(() => {}, []);

  return <Presenter />;
}

export default Interactor;
