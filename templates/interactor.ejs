//import { useEffect } from 'react';
import Presenter from './<%=name%>Presenter';
//import { useReduxState, useDispatch, myReducerName } from '../../controllers';
//import { TMProps<%=(name.charAt(0).toUpperCase() + name.slice(1))%> } from './<%=name%>Type';
//import {} from '../../types';

const Interactor = () => {
  //const dispatch = useDispatch();

  //useEffect(() => {}, []);

  return <Presenter />;
}

export default Interactor;
