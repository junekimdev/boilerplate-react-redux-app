import { AxiosResponse } from 'axios';
import {} from '../../types';

const <%=name%>Const = {
<% for (let i=0; i<funcnames.length; i++) {-%>
  <%=funcnames[i]%>: '<%=typenames[i]%>',
<% } -%>
};

const <%=name%>Action = {
<%_ for (let i=0; i<funcnames.length; i++) {-%>
  <%=funcnames[i]%>: (payload?: any) => ({
    type: <%=name%>Const.<%=funcnames[i]%>,
    payload,
  }),
<%_ } -%>
};

export default { const: <%=name%>Const, action: <%=name%>Action };
